diff --git a/third_party/blink/renderer/core/frame/navigator_id.cc b/third_party/blink/renderer/core/frame/navigator_id.cc
index 22243ebf5e..72061f9b30 100644
--- a/third_party/blink/renderer/core/frame/navigator_id.cc
+++ b/third_party/blink/renderer/core/frame/navigator_id.cc
@@ -31,6 +31,9 @@
 
 #include "third_party/blink/renderer/core/frame/navigator_id.h"
 
+#include "base/command_line.h"  // UXR: persona via renderer cmdline switches
+#include "base/uxr_config.h"
+
 #include "base/feature_list.h"
 #include "build/build_config.h"
 #include "third_party/blink/public/common/features.h"
@@ -59,6 +62,12 @@ String NavigatorID::appVersion() {
 }
 
 String NavigatorID::platform() const {
+  // UXR Layer 2: persona-driven via renderer cmdline switch (whitelisted in
+  // render_process_host_impl.cc). Web-undetectable.
+  {
+    if (base::UxrConfig::GetInstance().Has("uxr-platform"))
+      return String(base::UxrConfig::GetInstance().Get("uxr-platform").c_str());
+  }
 #if BUILDFLAG(IS_MAC)
   // Match Safari and Mozilla on Mac x86.
   return "MacIntel";
